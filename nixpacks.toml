# Nixpacks configuration for Railway
# This tells Railway to use Node.js 20+ and includes build tools

[phases.setup]
nixPkgs = ["nodejs-20_x", "python3", "gcc", "make"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm run build:next"]

[start]
cmd = "npm start"

